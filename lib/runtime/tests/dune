(test
  (name test_compile)
  (libraries tesserae alcotest))

(test
  (name test_nvrtc)
  (libraries tesserae alcotest base))

(test
  (name test_gpu_buffer)
  (libraries tesserae alcotest base))

(test
  (name test_runtime)
  (libraries tesserae alcotest)
  (link_flags (-cclib -L/usr/local/cuda/lib64 -cclib -lcuda -cclib -lcudart)))
